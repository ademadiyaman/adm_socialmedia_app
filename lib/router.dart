import 'dart:io';

import 'package:adm_socialmedia_app/app/confirm_status_contact.dart';
import 'package:adm_socialmedia_app/error.dart';
import 'package:adm_socialmedia_app/model/user.dart';
import 'package:adm_socialmedia_app/viewmodel/chat_view_model.dart';
import 'package:flutter/material.dart';
/*
Route<dynamic> generateRoute(RouteSettings settings) {
  switch (settings.name) {
    case ConfirmStatusScreen.routeName:
      final file = settings.arguments as File;
      final user = settings.arguments as ChatViewModel;
      return MaterialPageRoute(
        builder: (context) => ConfirmStatusScreen(
          file: file,
          //  user: user,
          //kullanici: kullanici,
        ),
      );
    default:
      return MaterialPageRoute(
        builder: (context) => const Scaffold(
          body: ErrorPage(error: 'This Page doesnt exist'),
        ),
      );
  }
}
*/
